# PostgreSQL Database Connection
DATABASE_URL=postgresql://username:password@localhost:5432/members_only_db

# Session Secret (use a strong random string). Generate with: openssl rand -base64 32
SECRET=your-super-secret-session-key-here